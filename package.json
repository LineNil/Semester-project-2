{
  "name": "semester-project-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile-scss": "node-sass --output-style compressed -o dist/css src/scss",
    "build": "sass src/scss:dist/css",
    "watch": "sass --watch src/scss:dist/css",
    "start": "live-server",
    "dev": "npm-run-all --parallel watch start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.0.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "node-sass": "^9.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.69.5"
  }
}
