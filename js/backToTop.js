/*document.addEventListener('DOMContentLoaded', function () {
  const backToTopBtn = document.getElementById('backToTopBtn');

  // Lytter etter scroll hendelser
  window.addEventListener('scroll', function () {
    // Viser eller skjuler tilbake til toppen-knappen basert på rullesituasjonen
    if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
      backToTopBtn.style.display = 'block';
    } else {
      backToTopBtn.style.display = 'none';
    }
  });

  // Lytter etter klikk hendelser på tilbake til toppen-knappen
  backToTopBtn.addEventListener('click', function () {
    // Ruller opp til toppen av siden med en jevn animasjon
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
});*/